# 小智桌面宠物 硬件接线说明 (Pinout Guide)

本文件详细列出了目前项目中支持的各款开发板及其传感器的引脚连接说明，旨在帮助开发者快速完成硬件搭建。

---

## 1. 核心总线设计：I2C 设备复用
为了节省 ESP32-S3 的 IO 资源，我们采用了 **I2C 总线复用** 方案。所有的传感器（APDS9960, AHT20）都挂载在系统已有的 I2C 总线上，通过设备地址进行区分。

### I2C 设备地址一览表
| 设备名称 | 功能 | I2C 地址 |
| :--- | :--- | :--- |
| **AHT20** | 温湿度传感器 | `0x38` |
| **APDS9960** | 接近/手势/光感 | `0x39` |
| **ES8311** | 音频解码 (DAC) | `0x18` |
| **ES7210** | 麦克风采集 (ADC) | `0x40 / 0x41` |
| **CST816S** | 屏幕触摸控制 | `0x15` |

---

## 2. 具体板型接线定义

### 方案 A: ESP-VoCat (桌面宠物专用板)
这是推荐的硬件方案，引脚定义如下：

| 功能 | 引脚名称 | ESP32-S3 引脚 (GPIO) |
| :--- | :--- | :--- |
| **I2C 总线** | **SDA** | **GPIO 2** (接所有传感器 SDA) |
| **I2C 总线** | **SCL** | **GPIO 1** (接所有传感器 SCL) |
| **音频 I2S** | MCLK / WS / BCLK | 42 / 39 / 40 |
| **音频 I2S** | DIN / DOUT | 15 / 41 |
| **屏幕 QSPI** | SCLK / CS / BL | 18 / 14 / 44 |
| **屏幕 QSPI** | D0 / D1 / D2 / D3 | 46 / 13 / 11 / 12 |
| **指示灯** | LED_G | 43 |

### 方案 B: ESP-BOX 系列 (官方开发套件)
适用于 ESP-BOX, ESP-BOX-Lite, ESP-BOX-3：

| 功能 | 引脚名称 | ESP32-S3 引脚 (GPIO) |
| :--- | :--- | :--- |
| **I2C 总线** | **SDA** | **GPIO 8** (接所有传感器 SDA) |
| **I2C 总线** | **SCL** | **GPIO 18** (接所有传感器 SCL) |
| **音频 I2S** | MCLK / WS / BCLK | 2 / 47 / 17 |
| **音频 I2S** | DIN / DOUT | 16 / 15 |
| **屏幕 SPI** | SCLK / MOSI / CS | 7 / 6 / 5 |
| **屏幕 SPI** | DC / RST / BL | 4 / 48 / 45 |

---

## 3. 传感器实际接线示意图

无论你使用哪种开发板，新增传感器的物理接线逻辑如下：

```text
[ESP32-S3]             [AHT20 模块]         [APDS9960 模块]
   3.3V   -----------+--- VCC ----------+--- VCC
   GND    -----------+--- GND ----------+--- GND
   SDA    -----------+--- SDA ----------+--- SDA
   SCL    -----------+--- SCL ----------+--- SCL
```

### 注意事项：
1. **电源电压**：所有传感器均需接入 **3.3V**。严禁接入 5V，否则会烧毁传感器或 ESP32 引脚。
2. **总线长度**：I2C 属于短距离通信，建议杜邦线长度不要超过 15cm，否则可能会因为信号完整性问题导致传感器初始化失败。
3. **上拉电阻**：如果自制的传感器模块没有上拉电阻，请在 SDA 和 SCL 引脚上各并联一个 4.7kΩ 的上拉电阻到 3.3V。
4. **中断引脚 (INT)**：目前代码通过轮询方式读取手势和温湿度，因此 APDS9960 的 INT 引脚可以保持悬空（NC）。

---

## 4. 故障排查
如果串口监视器输出 `[SensorManager] APDS9960 not found` 或 `AHT20 not found`：
- 请检查 SDA 和 SCL 是否接反。
- 请检查 3.3V 电源是否稳压。
- 确认传感器地址是否与代码中的 `0x38/0x39` 一致。
